import React from 'react';import ReactModal from '../common/ReactModal.jsx';import {useApp} from '../../context/AppContext.jsx';
export default function IcebreakerModal(){const {modal}=useApp();return <ReactModal name="icebreaker" title={modal?.person?`Start a chat with ${modal.person}`:'Icebreaker AI'}><p style={{color:'var(--text-muted)'}}>Choose an icebreaker to start the conversation.</p><div style={{display:'grid',gap:10}}>{['What brings you here?','What are you into lately?','Coffee or chai?'].map(x=><button className="btn-connect" key={x}>{x}</button>)}</div></ReactModal>}
