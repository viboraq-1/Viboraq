import React,{useEffect} from 'react';
import {useApp} from '../../context/AppContext.jsx';
export default function ReactModal({name,title,children,className='',onRequestClose}){const {modal,closeModal}=useApp();const open=modal?.name===name;useEffect(()=>{if(!open)return;const prev=document.body.style.overflow;const key=e=>{if(e.key==='Escape')(onRequestClose||closeModal)()};document.body.style.overflow='hidden';document.addEventListener('keydown',key);return()=>{document.body.style.overflow=prev;document.removeEventListener('keydown',key)}},[open,closeModal,onRequestClose]);if(!open)return null;const close=onRequestClose||closeModal;return <div className={`mobile-modal ${className}`} role="dialog" aria-modal="true"><div className="mobile-modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&close()}/><section className="mobile-modal-sheet"><div className="sheet-handle"/><button className="sheet-close" onClick={close} aria-label="Close">×</button>{title&&<h2>{title}</h2>}{children}</section></div>}
